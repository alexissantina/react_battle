var React = require('react');
var ReactDOM = require('react-dom');
require('./index.css');

// state

// lifecycle event

// UI (the only one that is required, set with the render method)

// Create a React component
class App extends React.component {
	render() {
		return (
			<div>
				Hello World!
			</div>
		)
	}
}